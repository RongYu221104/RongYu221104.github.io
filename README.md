# RongYu's Notes

Personal lecture archive and tools site built with Astro and deployed on GitHub Pages.

## Commands

```bash
pnpm install
pnpm dev
pnpm check
pnpm build
```

Source assets are kept in the local `input/` directory. Prepared public assets live under `public/`.
